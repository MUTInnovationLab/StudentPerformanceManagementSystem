export interface Student {
    id: number;  
    studentNumber: string; // Ensure it's a string
    name: string;
    surname: string;
    email: string;
    course: string;
    year: string;
    department: string;
    enrollmentDate: string;
  }
  